<?xml version="1.0" encoding="utf-8"?>
<server type="storage" supports="*.*">
    <name>External Link</name>
    <description>Link directly to a file on another server</description>

    <fieldset name="params">
        <fields name="params">
            <field name="baseurl" type="text" inputclass="input-medium" label="*BASE_URL*" description="*BASE_URL*"/>
        </fields>
    </fieldset>
</server>
