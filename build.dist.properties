; ==============================================================================
; build.dist.properties
;
; Auto-managed by `composer cwm-sync-configs`. Do not edit by hand.
; To change defaults across all CWM repos, edit
;   cwm-build-tools/templates/build.properties.tmpl
; and re-run `composer cwm-sync-configs` in each consumer.
;
; Per-developer state lives in build.properties (gitignored). Copy this
; file to build.properties and either edit by hand or run `composer setup`.
; ==============================================================================

; build.properties — local Joomla installs for cwm-build-tools dev commands.
;
; This file is the per-developer mate to cwm-build.config.json:
;   - cwm-build.config.json (committed) — what the project IS:
;       extension layout, manifest paths, optional dev.links[].
;   - build.properties (gitignored) — where YOUR dev environment runs:
;       paths to your Joomla installs, DB credentials, admin credentials.
;
; To set up the first time:
;   cp build.dist.properties build.properties
; Then either edit by hand or run the wizard:
;   composer setup
;
; ALWAYS keep this gitignored. It contains DB and admin passwords.

; Each install has a role:
;   role = dev  — symlink-style working install. `cwm-link` deploys here.
;   role = test — artifact-style install. `cwm-install-zip` deploys a built
;                 zip here and `cwm-verify --target test` queries it.
; Default role is `dev` if omitted. Multiple installs may share a role
; (e.g. j5 and j6 both as dev) — commands apply to each in turn.

; Comma-separated list of install ids — must match the section names below.
installs = j5, j6, j5-test

[j5]
role = dev
path = /path/to/joomla5
url = https://j5-dev.local
version = 5.4.2
db_host = localhost
db_user =
db_pass =
db_name =
admin_user = admin
admin_pass = admin
admin_email = admin@example.com

[j6]
role = dev
path = /path/to/joomla6
url = https://j6-dev.local
version = 6.0.0
db_host = localhost
db_user =
db_pass =
db_name =
admin_user = admin
admin_pass = admin
admin_email = admin@example.com

; Optional: a separate Joomla install for verifying the SHIPPED artifact.
; `cwm-install-zip` deploys the built dist zip into this install (rather
; than symlinking source) so you can exercise the install scriptfile,
; manifest declarations, and schema migrations end-to-end before release.
; Remove this section if you don't run a separate test install.
[j5-test]
role = test
path = /path/to/joomla5-test
url = https://j5-test.local
version = 5.4.2
db_host = localhost
db_user =
db_pass =
db_name =
admin_user = admin
admin_pass = admin
admin_email = admin@example.com

; Per-developer absolute paths to CWM siblings declared in
; cwm-build.config.json under dev.cwmSiblings. Written by `composer setup`
; — leave empty here and let the wizard manage entries, or hand-edit
; when you move sibling checkouts. Each entry's key is the Composer
; package name; the value is the local checkout path.
;
; [paths]
; joomla-bible-study/lib-cwmscripture = /Users/you/GitHub/lib_cwmscripture
; cwm/scripture-links                 = /Users/you/GitHub/CWMScriptureLinks
